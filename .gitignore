node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Capacitor / Android build artifacts
android/.gradle/
android/.idea/
android/app/build/
android/build/
android/local.properties
android/app/release/
android/keystore.properties

# Android signing keystore (sensitive secret — NEVER commit)
keystore/
keystore/*.keystore

# أسرار التوقيع المحلية (يقرؤها script/release-all.sh)
.release-signing

# Release build artifacts (APKs / AABs / desktop / IPA)
release/apk/
release/play/
release/desktop/
release/ipa/
release/*.apk
release/*.aab
release/SHA256SUMS*.txt
release/keystore/

# Firebase hosting local cache
.firebase/

# Tauri — build outputs & generated schemas
src-tauri/target/
src-tauri/gen/
src-tauri/icons/android/
src-tauri/icons/ios/

# iOS (Capacitor) — build outputs
ios/App/Pods/
ios/App/DerivedData/
ios/App/build/
ios/App/App.xcworkspace/
*.xcuserstate

# سجل تشغيل البروتوكول الإلزامي
.release-last.log